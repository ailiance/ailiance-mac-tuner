# Probe test — matching Qwen3.5 proven config
model: models/Qwen3.6-35B-A3B
data: /Users/clems/ailiance/data/hf-traced/python
train: true
fine_tune_type: lora
optimizer: adamw
num_layers: -1
lora_parameters:
  rank: 64
  scale: 2.0
  dropout: 0.05
batch_size: 4
iters: 30
val_batches: 5
learning_rate: 1e-5
steps_per_report: 10
steps_per_eval: 30
save_every: 30
grad_accumulation_steps: 2
max_seq_length: 2048
grad_checkpoint: true
adapter_path: /tmp/qwen36-probe2
seed: 42
